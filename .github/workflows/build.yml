name: Build Android APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
      - name: 1. 拉取代码
        uses: actions/checkout@v4

      - name: 2. 配置 Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 3. 配置 Android NDK r26b
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: r26b
          add-to-path: true

      - name: 4. 安装 Qt 6.5.0
        # 🟢 修正仓库名：miurahr/aqtinstall
        uses: miurahr/aqtinstall@v3
        with:
          version: 6.5.0
          host: linux
          target: android
          arch: android_arm64_v8a
          # 同时下载桌面工具，解决 QT_HOST_PATH 问题
          modules: 'qt5compat qtshadertools'

      - name: 5. 编译 APK
        run: |
          mkdir build_temp
          cd build_temp

          # 🟢 aqtinstall 会自动设置环境变量
          # Qt6_DIR 通常指向 .../android_arm64_v8a/lib/cmake/Qt6
          # 我们直接利用它找到工具链
          
          cmake .. \
            -DCMAKE_TOOLCHAIN_FILE=${{ env.Qt6_DIR }}/qt.toolchain.cmake \
            -DANDROID_NDK_ROOT=${{ steps.setup-ndk.outputs.ndk-path }} \
            -DANDROID_SDK_ROOT=$ANDROID_HOME \
            -DQT_HOST_PATH=${{ env.Qt6_DIR }}/../../../../gcc_64 \
            -DANDROID_ABI=arm64-v8a \
            -Wno-dev
            
          cmake --build . --parallel
          
          # 查找生成的 apk 并移动到当前目录
          find . -name "*.apk" -exec cp {} app-release.apk \;

      - name: 6. 上传成品
        uses: actions/upload-artifact@v4
        with:
          name: My-Android-App
          path: build_temp/app-release.apk
