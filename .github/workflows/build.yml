name: Build Android APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-22.04
    
    steps:
      - name: 1. 拉取代码
        uses: actions/checkout@v4

      - name: 2. 配置 Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 3. 配置 Android NDK r26b
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: r26b
          add-to-path: true

      - name: 4. 安装 Qt 6.5.0
        # 🟢 修正：使用 v2 版本，这是目前该插件的主流用法
        uses: miurahr/aqtinstall@v2.2
        with:
          version: 6.5.0
          host: linux
          target: android
          arch: android_arm64_v8a
          # 确保安装了基础工具包
          modules: 'qtshadertools'

      - name: 5. 编译 APK
        run: |
          mkdir build_temp
          cd build_temp

          # 🟢 aqtinstall v2 会自动把 Qt 安装路径存入 $QT_ROOT 环境变量
          # 我们据此找到 Android 库路径和 Host 工具路径
          
          ANDROID_QT_PATH=$QT_ROOT/android_arm64_v8a
          HOST_QT_PATH=$QT_ROOT/gcc_64
          
          cmake .. \
            -DCMAKE_TOOLCHAIN_FILE=$ANDROID_QT_PATH/lib/cmake/Qt6/qt.toolchain.cmake \
            -DANDROID_NDK_ROOT=${{ steps.setup-ndk.outputs.ndk-path }} \
            -DANDROID_SDK_ROOT=$ANDROID_HOME \
            -DQT_HOST_PATH=$HOST_QT_PATH \
            -DANDROID_ABI=arm64-v8a \
            -Wno-dev
            
          cmake --build . --parallel
          
          # 查找生成的 apk 并复制
          find . -name "*.apk" -exec cp {} app-release.apk \;

      - name: 6. 上传成品
        uses: actions/upload-artifact@v4
        with:
          name: My-Android-App
          path: build_temp/app-release.apk
